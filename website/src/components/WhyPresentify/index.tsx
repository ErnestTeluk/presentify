import React from 'react';

import { StyledSection, StyledTitle } from './WhyPresentify.styled';

export const WhyPresentify = (): JSX.Element => (
  <StyledSection>
    <StyledTitle>Why Presentify?</StyledTitle>
  </StyledSection>
);
